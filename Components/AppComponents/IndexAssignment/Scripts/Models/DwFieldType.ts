namespace DW.IndexAssignment.Models {
    export enum DwFieldType { //DON'T CHANGE THE ORDER
        Text,
        Numeric,
        Memo,
        Date,
        Keyword,
        Decimal,
        DateTime,
        Table
    }
}